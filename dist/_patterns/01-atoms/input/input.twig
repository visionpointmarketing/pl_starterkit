<label for="{{ input_id }}">
	{{ input_label }}
	<input aria-label="{{ input_label }}" data-parsley-trigger="change" id="{{ input_id }}" type="{{ input_type }}" placeholder="{{ input_label }}" />
</label>